# Filesystem test

App takes filename of any file (can be shared) and performs replacement of character specified in terminal. Final file can be seen in drive/procX. App pauses on first replacement so that user can see new file being created in drive/procX in case of shared file.